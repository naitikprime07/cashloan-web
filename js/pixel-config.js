/* Shared static tracking configuration. Pixel IDs are public, not secrets. */
window.PIXEL_CONFIG = window.PIXEL_CONFIG || {
  enabled: true,
  meta: {
    pixelId: "2355848365180702",
    externallyInitialized: false // True only if another integration owns fbq init.
  }
};
